wiremock=true

api.root=http://localhost:8080

rest.connection.connection-request-timeout=500
rest.connection.connect-timeout=500
rest.connection.read-timeout=500

connectionAttemptCount=2
connectionRetryDelay=500
